{
  "name": "snarl-sopi-backend",
  "version": "4.99.0",
  "description": "Snarl & Sopi operator backend — v4.99: Payday probe deep-tests the invoice→customer filter param and the customer→ssn filter param using a real record from /invoices (self-contained). Confirmed: invoices=/invoices (envelope key invoices, amount=amountIncludingVat, status UPPERCASE, invoiceDate/dueDate/paidDate), no /payments endpoint (payments embedded per invoice), customers=/customers. Includes v4.96 Billing portal.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}