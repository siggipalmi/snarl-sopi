{
  "name": "snarl-sopi-backend",
  "version": "4.66.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (kiosk support contact now derives from the assigned operator: profile.supportEmail/phone fall back to operator.contactEmail/Phone, house value only when blank; reassigning a machine clears its support contact so it re-derives for the new operator)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
