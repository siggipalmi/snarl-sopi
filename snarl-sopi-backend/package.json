{
  "name": "snarl-sopi-backend",
  "version": "4.69.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (product database reconciliation: Catalog database-check panel lists products loaded on machines but missing from the DB, per operator; one-tap add auto-fills name/image/price from Weimi via /products/enrich; bilingual migration help)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
