{
  "name": "snarl-sopi-backend",
  "version": "4.75.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (product-details import now machine-aware: pick a machine, match the sheet against products actually loaded on it, and auto-create a DB row under the device code when missing so details land where the kiosk reads them)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
