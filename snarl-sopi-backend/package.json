{
  "name": "snarl-sopi-backend",
  "version": "5.14.0",
  "description": "Snarl & Sopi operator backend — v5.14: idle preview multi-buy card shows the product name (with image) when the deal targets a single product; thumbnail row kept for multi-product. Frontend-only on v5.13 (idle-promo + lease v5.12).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}