{
  "name": "snarl-sopi-backend",
  "version": "5.33.0",
  "description": "Snarl & Sopi operator backend — v5.33: kiosk sales now resolve product name from the planogram by goodsId at ingest (kiosk cart lines carry no name), so kiosk-stock orders show the product instead of (unknown). Cumulative over v5.32.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}