{
  "name": "snarl-sopi-backend",
  "version": "4.78.0",
  "description": "Expiry tracking — fix. The Expiry slot editor got stuck on \"loading\" because /products and /products/catalog wrap their array as {products:[...]}; the page now reads that shape, and /products now returns the perishable flag so the Track-expiry toggle reflects real state. Builds on v4.76/4.77.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}