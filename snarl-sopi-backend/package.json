{
  "name": "snarl-sopi-backend",
  "version": "5.04.0",
  "description": "Snarl & Sopi operator backend — v5.04: ledger invoice rows now read \"Invoice #<num>\" instead of echoing Payday's stale free-text description (which carried the Aug #187 adjustment text across later invoices). Billing nav moved into the System group. Builds on v5.03.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}