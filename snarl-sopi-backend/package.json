{
  "name": "snarl-sopi-backend",
  "version": "5.43.0",
  "description": "Snarl & Sopi operator backend — v5.43: serve GET /app-update BARE (no ok/data envelope) so kiosk updaters that read manifest fields at the top level parse it, letting pre-0.72 field machines self-heal over the air instead of needing a site visit. Cumulative over v5.42.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}