{
  "name": "snarl-sopi-backend",
  "version": "5.08.0",
  "description": "Snarl & Sopi operator backend — v5.08: fix empty machine scope picker in deals — the list was guarded on window.DB (DB is a lexical global, so window.DB was undefined → always empty). Now reads DB directly and hydrates the fleet on demand if Deals is opened before Machines. Builds on v5.07.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}