{
  "name": "snarl-sopi-backend",
  "version": "4.72.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (unified kiosk-screen designer: per-machine grid reorder via gridOrder + hero tiles + ads + sold-out badges/slot labels, saved together; gridOrder served in kiosk config; 'design kiosk screen' link on machine detail; Hero & Featured reframed for bulk; bilingual Help for controls + designer)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
