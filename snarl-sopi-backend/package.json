{
  "name": "snarl-sopi-backend",
  "version": "5.35.0",
  "description": "Snarl & Sopi operator backend — v5.35: restart now also delivered via the pull-queue (restart_app/restart_machine command types, with ack + history) in addition to the config timestamp, so it works regardless of which channel the kiosk implements. Cumulative over v5.34.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}