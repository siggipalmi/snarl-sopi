{
  "name": "snarl-sopi-backend",
  "version": "5.19.0",
  "description": "Snarl & Sopi operator backend — v5.19: lease reseed/free now report real result counts and surface server errors (diagnose all-assigned + silent reseed)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}