{
  "name": "snarl-sopi-backend",
  "version": "5.17.0",
  "description": "Snarl & Sopi operator backend — v5.17: product picker pulls image/price from the layout when the catalog row lacks them, and collapses duplicate catalog records for one product (in-stock planogram id vs an orphaned out-of-stock catalog id) into a single entry, carrying the image over. Builds on v5.16.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}