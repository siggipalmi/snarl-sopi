{
  "name": "snarl-sopi-backend",
  "version": "5.24.0",
  "description": "Snarl & Sopi operator backend — v5.24: kiosk stock ownership (stockSource flag, Weimi-free restock writing the planogram directly with restockAt, sales ingest decrements the layout with stale-vend timestamp guard, stock-source toggle in display settings).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}