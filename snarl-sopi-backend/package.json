{
  "name": "snarl-sopi-backend",
  "version": "4.65.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (hero redesign: machine-first tap-to-assign from the machine's own products into 2 left/right slots; batch-apply a hero across machines that stock it, append-with-dedupe + replace-all)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
