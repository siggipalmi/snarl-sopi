{
  "name": "snarl-sopi-backend",
  "version": "4.81.0",
  "description": "Snarl & Sopi operator backend — v4.81 makes the Operators view expandable: each operator row drops down its machines (online dot, device code, stock fill %, last-restock time), grouped client-side from the existing machine list. No backend change. (v4.80: remote machine command queue + drop-sensor mode, contract v0.5.)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}