{
  "name": "snarl-sopi-backend",
  "version": "5.03.0",
  "description": "Snarl & Sopi operator backend — v5.03: billing ledger fix — credit/refund movements were borrowing the lease label (looked like duplicated charges); now labeled distinctly. Added a Refresh button + cache-buster so the ledger re-pulls live from Payday on demand. Builds on v5.02 Payday link hardening.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}