{
  "name": "snarl-sopi-backend",
  "version": "5.30.0",
  "description": "Snarl & Sopi operator backend — v5.30: machine config now exposes a per-goodsId stock map (summed across bays, includes 0) so the kiosk can re-baseline its local working count. Cumulative over v5.29 (sales envelope acceptance).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}