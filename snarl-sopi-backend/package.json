{
  "name": "snarl-sopi-backend",
  "version": "5.34.0",
  "description": "Snarl & Sopi operator backend — v5.34: debug/commands endpoint now reports restartApp/restartMachine timestamps + configVersion + stockSource, to verify restart commands are issued (delivery contract is config.commands.restartApp/restartMachine; kiosk must act on them). Cumulative over v5.33.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}