{
  "name": "snarl-sopi-backend",
  "version": "5.28.0",
  "description": "Snarl & Sopi operator backend — v5.28: enriched offers[] for kiosk grid badging (expiry daysLeft + discountedPriceIsk; markdown discountedPriceIsk; multibuy qty/totalKr; combo comboId + group member lists). Cumulative over v5.27 (/sales received-keys diagnostic) and v5.26 (deal picker planogram candidates).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}