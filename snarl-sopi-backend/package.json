{
  "name": "snarl-sopi-backend",
  "version": "5.12.0",
  "description": "Snarl & Sopi operator backend — v5.12: extends POST /api/v1/leases/free with bulk modes — {assignedTo} frees all units for a name, {reseed:true} wipes and restores the exact starting inventory (40 units, 5 used). For cleaning up test claims. Builds on v5.11.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}