{
  "name": "snarl-sopi-backend",
  "version": "4.88.0",
  "description": "Snarl & Sopi backend v4.88 — order-sync diagnosis: rawOrdersDiagnostic now tries GET /ext/query-order-list with date range + pagination (the case the old probe missed) to find which params return orders. Removed shadowed duplicate debug route. Incl v4.86 favicon + mobile fixes.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}