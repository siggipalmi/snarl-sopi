{
  "name": "snarl-sopi-backend",
  "version": "4.87.0",
  "description": "Snarl & Sopi backend v4.87 — added read-only /debug/weimi-orders probe to diagnose empty revenue (shows what Weimi returns for a device order query). Includes v4.86 favicon + mobile-overflow fixes.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}