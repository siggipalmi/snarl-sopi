{
  "name": "snarl-sopi-backend",
  "version": "5.11.0",
  "description": "Snarl & Sopi operator backend — v5.11: adds POST /api/v1/leases/free to reset a lease unit back to available (for clearing test claims). Builds on v5.10 lease-unit inventory + claim.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}