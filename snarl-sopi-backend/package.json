{
  "name": "snarl-sopi-backend",
  "version": "5.31.0",
  "description": "Snarl & Sopi operator backend — v5.31: Orders tab now fetches from /api/v1/orders (kiosk sales) instead of Weimi; Machines tab shows revenue chart with 1D/7D/30D/90D toggles. Cumulative over v5.30.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}