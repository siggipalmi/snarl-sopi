{
  "name": "snarl-sopi-backend",
  "version": "4.62.0",
  "description": "Snarl & Sopi frá AG Vending — Operator Backend (hero/featured products wired end-to-end: product picker + saves to kiosk config + live preview; kiosk-config debug endpoint)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}
