{
  "name": "snarl-sopi-backend",
  "version": "4.94.0",
  "description": "Snarl & Sopi backend v4.94 — corrected Payday auth to the real flow: POST clientId/clientSecret to /auth/token for a 24h Bearer token (no refresh token). Needs only PAYDAY_CLIENT_ID + PAYDAY_CLIENT_SECRET. Billing portal foundation otherwise as v4.93.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "@sendgrid/mail": "^8.1.4",
    "better-sqlite3": "^11.3.0",
    "@aws-sdk/client-s3": "^3.1061.0"
  }
}