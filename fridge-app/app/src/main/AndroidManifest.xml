<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"
        tools:ignore="ProtectedPermissions" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <!--
        *** PACKAGE VISIBILITY. Android 11 hides other apps by default, so
        getLaunchIntentForPackage() returns null for TeamViewer even when it IS
        installed — and the admin sheet's "open teamviewer" button would always
        fall through to the download page. Declaring it makes the check honest.
    -->
    <queries>
        <package android:name="com.teamviewer.quicksupport.market" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".FridgeApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/Theme.SnarlFridge"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true">

        <!-- Landscape: the fridge runs a 22" landscape panel (vs the coil app's
             portrait screen). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|locale|layoutDirection"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.SnarlFridge">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Home category: on a provisioned (Device Owner) machine this lets
                 the app be the default Home so a power-cycle boots straight in. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Device-admin receiver for Device Owner / Lock Task Mode.
             adb shell dpm set-device-owner isl.snudursopi.fridge/.KioskAdminReceiver -->
        <receiver
            android:name=".KioskAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Power-cut recovery: relaunch after a reboot so the machine comes
             back without anyone attending it. -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Watchdog tick. MUST be manifest-registered (not runtime): Android
             starts a fresh process to deliver this even when the app is dead,
             which is exactly the case we need to recover from. -->
        <!-- PackageInstaller commit result for a self-update. Not exported: only
             the system delivers to it, via our own PendingIntent. -->
        <receiver
            android:name=".update.UpdateResultReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="isl.snudursopi.fridge.UPDATE_RESULT" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".WatchdogReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="isl.snudursopi.fridge.WATCHDOG_TICK" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
